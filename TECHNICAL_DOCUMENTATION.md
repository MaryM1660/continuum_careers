# 📚 Техническая документация - Continuum Careers

> **Полное руководство по архитектуре, разработке и развертыванию платформы Continuum Careers**

---

## 📖 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Технический стек](#технический-стек)
3. [Архитектура системы](#архитектура-системы)
4. [Установка и настройка](#установка-и-настройка)
5. [Структура проекта](#структура-проекта)
6. [Разработка компонентов](#разработка-компонентов)
7. [Стили и дизайн](#стили-и-дизайн)
8. [Скрипты и команды](#скрипты-и-команды)
9. [Развертывание](#развертывание)
10. [Лучшие практики](#лучшие-практики)
11. [Устранение неполадок](#устранение-неполадок)

---

## 🎯 Обзор проекта

### Что такое Continuum Careers?

**Continuum Careers** — это современная SaaS-платформа с ИИ-помощником для профессионального развития карьеры. Продукт предоставляет:

- 🤖 **Голосовой ИИ-коуч** — трансформирует опыт в стратегическое позиционирование
- 📋 **Генерацию документов** — резюме, сопроводительные письма из единого профиля
- 🎯 **Множественные карьерные пути** — поддержка разных направлений развития
- 💼 **Подготовку к собеседованиям** — персонализированные рекомендации
- 🌍 **Поддержка глобальных рынков** — США, Великобритания, ЕС

### Назначение проекта

Проект представляет собой **лэндинг-пейдж** (landing page) для привлечения пользователей, демонстрирующий возможности платформы и стимулирующий к регистрации.

### Целевая аудитория

- Профессионалы, ищущие новую работу
- Люди, переходящие на новую карьеру
- Специалисты, желающие оптимизировать поиск работы
- HR-специалисты и рекрутеры

---

## 🛠️ Технический стек

### Основные технологии

| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **Astro** | 4.16.0 | Генератор статических сайтов (SSG) |
| **TypeScript** | 5.x | Статическая типизация |
| **Tailwind CSS** | 3.4.0 | Утилитарные CSS стили |
| **Node.js** | 18+ | Runtime |

### Зависимости проекта

```json
{
  "dependencies": {
    "astro": "^4.16.0",
    "@astrojs/tailwind": "^5.1.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "tailwindcss": "^3.4.0",
    "@types/node": "^20.x"
  }
}
```

### Поддерживаемые браузеры

- Chrome/Edge 90+
- Firefox 88+
- Safari 15+
- Mobile browsers (iOS Safari 15+, Chrome Mobile)

---

## 🏗️ Архитектура системы

### Концепция архитектуры

Continuum Careers использует **архитектуру SSG (Static Site Generation) с прогрессивным улучшением**:

```
┌─────────────────────────────────────────────┐
│      HTML/Astro (Server-rendered)           │
│  (Статически генерируется при сборке)       │
└────────────────┬────────────────────────────┘
                 │
┌─────────────────▼────────────────────────────┐
│    Tailwind CSS (Утилитарные стили)         │
│  (Responsive, Dark Mode, Анимации)          │
└────────────────┬────────────────────────────┘
                 │
┌─────────────────▼────────────────────────────┐
│  Vanilla JavaScript (Интерактивность)       │
│  (Без фреймворков, минимальный overhead)    │
└────────────────┬────────────────────────────┘
                 │
┌─────────────────▼────────────────────────────┐
│   Компонентная архитектура (13 Astro)       │
│  (Переиспользуемые, модульные части)        │
└─────────────────────────────────────────────┘
```

### Ключевые архитектурные решения

#### 1. **Отсутствие фреймворков JavaScript**
- Нулевой оверхед во время выполнения
- Максимальная скорость загрузки
- Простая отладка и поддержка

#### 2. **Компонентная система**
Каждый компонент содержит:
- Разметку (HTML/Astro)
- Локальные стили (Tailwind/CSS)
- Минимальную интерактивность (vanilla JS)

#### 3. **Система стилей слоями**
```css
@tailwind base;      /* HTML reset, базовые стили */
@tailwind components; /* Переиспользуемые компоненты */
@tailwind utilities;  /* Однозначные утилиты */
```

#### 4. **Dark Mode поддержка**
- Переключение через localStorage
- Класс-стратегия: `dark:` префикс в Tailwind
- Сохранение предпочтения пользователя

### Схема потока данных

```
Пользователь
    ↓
[Браузер загружает статический HTML]
    ↓
[CSS загружается параллельно]
    ↓
[JS инициализирует интерактивность]
    ↓
[Intersection Observer слушает прокрутку]
    ↓
[Анимации запускаются при прокрутке]
    ↓
[User взаимодействует с CTA]
```

---

## 📥 Установка и настройка

### Требования

- **Node.js** 18.0.0 или выше
- **npm** 9.0.0 или выше
- **Git** для контроля версий

### Проверка окружения

```bash
# Проверьте версию Node.js
node --version
# Ожидаемый результат: v18.0.0 или выше

# Проверьте версию npm
npm --version
# Ожидаемый результат: 9.0.0 или выше
```

### Установка зависимостей

```bash
# 1. Перейдите в папку проекта
cd continuum_careers

# 2. Установите зависимости
npm install

# 3. Проверьте установку
npm list astro tailwindcss
```

### Переменные окружения

В проекте нет критических переменных окружения, но вы можете создать `.env` для будущих интеграций:

```bash
# .env (опционально)
VITE_API_URL=http://localhost:3000
VITE_ENVIRONMENT=development
```

### Инициализация проекта для разработки

```bash
# 1. Установите зависимости (если еще не сделано)
npm install

# 2. Запустите dev сервер
npm run dev

# 3. Откройте браузер на http://localhost:3000
```

---

## 📂 Структура проекта

### Обзор файловой структуры

```
continuum_careers/
├── src/
│   ├── components/          # 13 переиспользуемых компонентов
│   │   ├── Header.astro     # Навигация и заголовок
│   │   ├── Hero.astro       # Главный баннер
│   │   ├── WhyContinuum.astro
│   │   ├── HowItWorks.astro
│   │   ├── Features.astro
│   │   ├── Expertise.astro
│   │   ├── Metrics.astro
│   │   ├── Testimonials.astro
│   │   ├── Pricing.astro
│   │   ├── FAQ.astro
│   │   ├── CTA.astro
│   │   ├── Footer.astro
│   │   └── SocialProof.astro
│   │
│   ├── layouts/
│   │   └── Layout.astro     # Главный макет (HTML оболочка)
│   │
│   ├── pages/
│   │   └── index.astro      # Главная страница (entry point)
│   │
│   └── styles/
│       └── global.css       # Глобальные стили Tailwind
│
├── public/                  # Статические ресурсы
│   ├── images/
│   └── icons/
│
├── astro.config.mjs         # Конфигурация Astro
├── tailwind.config.mjs      # Конфигурация Tailwind
├── tsconfig.json            # Конфигурация TypeScript
├── package.json             # Зависимости и скрипты
└── README.md               # Основная документация
```

### Описание ключевых файлов

#### `src/pages/index.astro` (Entry Point)

Главная страница приложения. Здесь компонуются все 13 компонентов в последовательность:

```astro
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Hero from '../components/Hero.astro';
// ... импорты остальных компонентов
---

<Layout>
  <Header />
  <Hero />
  <WhyContinuum />
  <!-- ... остальные компоненты -->
  <Footer />
</Layout>
```

#### `src/layouts/Layout.astro`

Корневой макет, содержит:
- HTML оболочка (`<html>`, `<head>`, `<body>`)
- Meta-теги (viewport, favicon, описание)
- Инициализация темы (dark/light mode)
- Глобальные скрипты

#### `src/styles/global.css`

Содержит:
- Директивы Tailwind (`@tailwind base`, `components`, `utilities`)
- Пользовательские компоненты (`.btn-primary`, `.glass`)
- Глобальные анимации
- Переменные CSS для цветов

#### `astro.config.mjs`

```javascript
import { defineConfig } from 'astro/config';
import tailwind from '@astrojs/tailwind';

export default defineConfig({
  integrations: [tailwind()],
  output: 'static',
  build: {
    format: 'directory'
  }
});
```

#### `tailwind.config.mjs`

Содержит:
- Пользовательскую палитру цветов
- Расширенные анимации
- Настройки dark mode
- Пользовательские шрифты

---

## 🧩 Разработка компонентов

### Жизненный цикл компонента Astro

```
┌──────────────────┐
│  Создание файла  │
│  (Component.astro)
└────────┬─────────┘
         │
┌────────▼──────────┐
│ Файл-фасад забора │ (разделение кода/содержимого ---)
└────────┬──────────┘
         │
┌────────▼──────────────────┐
│ Использование при сборке  │
│ (Компилируется в HTML)    │
└────────┬──────────────────┘
         │
┌────────▼──────────────────┐
│ Вывод в браузер           │
│ (Статический HTML)        │
└───────────────────────────┘
```

### Структура типичного компонента

```astro
---
// 1. ФАСАД (Code Fence) - выполняется на сервере
import type { Props } from './types';

interface Props {
  title?: string;
  items?: Array<{ id: string; label: string }>;
}

const { title = 'Default', items = [] } = Astro.props;

// Логика компонента на сервере
const processedItems = items.map(item => ({
  ...item,
  label: item.label.toUpperCase()
}));
---

<!-- 2. HTML РАЗМЕТКА - рендерится на сервере -->
<section class="py-16 px-4 md:px-8">
  <div class="max-w-7xl mx-auto">
    <h2 class="text-4xl font-bold mb-8">{title}</h2>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {processedItems.map(item => (
        <div class="p-6 bg-white dark:bg-gray-800 rounded-lg shadow">
          <p>{item.label}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- 3. СТИЛИ - включаются через Tailwind -->
<style>
  section {
    @apply bg-gradient-to-br from-purple-50 to-blue-50 dark:from-gray-900 dark:to-gray-800;
  }
</style>
```

### Примеры компонентов

#### Пример 1: Простой компонент (SocialProof.astro)

```astro
---
// Минимальная логика, в основном разметка
---

<div class="flex items-center justify-center gap-4">
  <p class="text-sm text-gray-600 dark:text-gray-400">
    Trusted by thousands of professionals
  </p>
  <div class="flex -space-x-2">
    <!-- Avatar images -->
  </div>
</div>

<style>
  /* Компонент-специфичные стили */
</style>
```

#### Пример 2: Сложный компонент (HowItWorks.astro)

```astro
---
interface Step {
  number: number;
  title: string;
  description: string;
  image: string;
}

const steps: Step[] = [
  {
    number: 1,
    title: "Sign up",
    description: "Create your account...",
    image: "/images/step-1.png"
  },
  // ... другие шаги
];
---

<section class="py-20">
  <div class="max-w-6xl mx-auto">
    <h2 class="text-5xl font-bold text-center mb-16">How It Works</h2>

    <div class="space-y-12">
      {steps.map((step, index) => (
        <div class={`flex gap-8 ${index % 2 === 1 ? 'flex-row-reverse' : ''}`}>
          <div class="flex-1">
            <span class="text-6xl font-bold text-purple-200 opacity-50">{step.number}</span>
            <h3 class="text-2xl font-bold mb-4">{step.title}</h3>
            <p class="text-gray-600 dark:text-gray-400">{step.description}</p>
          </div>
          <div class="flex-1">
            <img src={step.image} alt={step.title} class="rounded-lg" />
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
```

### Правила разработки компонентов

✅ **Делайте:**
- Используйте TypeScript для типизации props
- Создавайте переиспользуемые компоненты
- Документируйте props через интерфейсы
- Изолируйте стили компонента
- Тестируйте responsive дизайн

❌ **Не делайте:**
- Не добавляйте глобальное состояние без необходимости
- Не используйте внешние фреймворки JS
- Не создавайте компоненты "на все случаи жизни"
- Не забывайте о dark mode поддержке
- Не игнорируйте a11y (доступность)

---

## 🎨 Стили и дизайн

### Палитра цветов

#### Основные бренд-цвета

```tailwind
primary:    #d946ef (Magenta/Purple)
secondary:  #f97316 (Coral/Orange)
accent:     #f59e0b (Amber)
```

#### Неутральные цвета (Cosmos Palette)

```tailwind
cosmos-50:   #fafafa
cosmos-100:  #f3f4f6
cosmos-200:  #e5e7eb
cosmos-300:  #d1d5db
cosmos-400:  #9ca3af
cosmos-500:  #6b7280
cosmos-600:  #4b5563
cosmos-700:  #374151
cosmos-800:  #1f2937
cosmos-900:  #111827
cosmos-950:  #030712 (Очень темный)
```

### Система компонентов Tailwind

#### Переиспользуемые компоненты CSS

```css
/* Кнопки */
.btn-primary {
  @apply px-6 py-3 bg-gradient-to-r from-purple-500 to-pink-500
         text-white font-semibold rounded-lg
         hover:shadow-lg transition-all duration-300;
}

.btn-secondary {
  @apply px-6 py-3 bg-white dark:bg-gray-800
         text-gray-900 dark:text-white border border-gray-200
         dark:border-gray-700 rounded-lg
         hover:bg-gray-50 dark:hover:bg-gray-700 transition-all;
}

/* Glass Morphism */
.glass {
  @apply bg-white/10 dark:bg-gray-800/10
         backdrop-blur-md border border-white/20
         dark:border-gray-700/20 rounded-xl;
}

.glass-light {
  @apply bg-white/5 dark:bg-gray-900/20
         backdrop-blur-sm border border-white/10;
}

/* Градиент текста */
.gradient-text {
  @apply bg-gradient-to-r from-purple-500 via-pink-500 to-amber-500
         bg-clip-text text-transparent;
}
```

### Анимации

```css
@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-20px); }
}

@keyframes pulse-glow {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.6; }
}

@keyframes fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes slide-up {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes wave {
  0%, 100% { transform: translateX(0); }
  50% { transform: translateX(10px); }
}
```

### Dark Mode

Dark mode работает через переключение класса `dark` на элементе `<html>`:

```javascript
// Инициализация темы
const isDark = localStorage.getItem('theme') === 'dark'
  || (!localStorage.getItem('theme') &&
      window.matchMedia('(prefers-color-scheme: dark)').matches);

if (isDark) {
  document.documentElement.classList.add('dark');
  localStorage.setItem('theme', 'dark');
}

// Переключение темы
function toggleTheme() {
  document.documentElement.classList.toggle('dark');
  const isDark = document.documentElement.classList.contains('dark');
  localStorage.setItem('theme', isDark ? 'dark' : 'light');
}
```

### Responsive Design Breakpoints

```tailwind
sm:  640px   (мобильные устройства)
md:  768px   (планшеты)
lg:  1024px  (ноутбуки)
xl:  1280px  (большие экраны)
2xl: 1536px  (очень большие экраны)
```

### Типография

Основной шрифт: **Inter** (Google Fonts)

```tailwind
text-xs:    12px (доп. информация)
text-sm:    14px (подзаголовки)
text-base:  16px (основной текст)
text-lg:    18px (акцентированный текст)
text-xl:    20px (подзаголовки)
text-2xl:   24px (заголовки секций)
text-4xl:   36px (главные заголовки)
text-5xl:   48px (огромные заголовки)
```

---

## 🚀 Скрипты и команды

### Основные команды

```bash
# Развитие
npm run dev

# Описание: Запускает локальный dev сервер на http://localhost:3000
# Горячая перезагрузка при сохранении файлов
# Используется для разработки и тестирования

# Продакшн сборка
npm run build

# Описание: Создает оптимизированный build для производства
# Результаты в папке `dist/`
# Минификация CSS/JS, оптимизация изображений
# Статический HTML вывод

# Предпросмотр продакшна
npm run preview

# Описание: Сервирует локально продакшн-build
# Позволяет тестировать перед развертыванием
# http://localhost:3000

# Проверка типов
npm run check

# Описание: Запускает TypeScript компилятор
# Проверяет наличие ошибок типизации
# Не генерирует выходные файлы
```

### Расширенные команды

```bash
# Чистка и переинсталляция
npm ci            # Clean install (для CI/CD)
npm prune         # Удаляет неиспользуемые пакеты

# Обновление зависимостей
npm update        # Обновляет все пакеты до последних minor версий
npm outdated      # Показывает устаревшие пакеты

# Проверка уязвимостей
npm audit         # Проверяет известные уязвимости
npm audit fix     # Автоматически исправляет уязвимости
```

---

## 📦 Развертывание

### Подготовка к развертыванию

#### 1. Проверка конфигурации

```bash
# Убедитесь, что все переменные окружения установлены
env | grep -E "^(NODE_|VITE_)"

# Проверьте версию Node.js
node --version  # Должно быть v18+

# Выполните проверку типов
npm run check
```

#### 2. Локальное тестирование

```bash
# Очистите предыдущий build
rm -rf dist/

# Создайте production build
npm run build

# Протестируйте build локально
npm run preview

# Откройте браузер на http://localhost:3000
```

### Развертывание на различные платформы

#### Vercel (Рекомендуется для Astro)

```bash
# 1. Установите Vercel CLI
npm install -g vercel

# 2. Авторизуйтесь
vercel login

# 3. Разверните проект
vercel deploy

# 4. Разверните в production
vercel --prod
```

**Конфигурация Vercel:**
```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "astro"
}
```

#### Netlify

```bash
# 1. Установите Netlify CLI
npm install -g netlify-cli

# 2. Авторизуйтесь
netlify login

# 3. Разверните
netlify deploy

# 4. Разверните в production
netlify deploy --prod
```

**netlify.toml:**
```toml
[build]
command = "npm run build"
publish = "dist"

[functions]
directory = "functions"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
```

#### GitHub Pages

```bash
# 1. Обновите astro.config.mjs
# site: "https://username.github.io/continuum_careers"

# 2. Установите зависимость для gh-pages
npm install -D gh-pages

# 3. Добавьте скрипт в package.json
"deploy": "npm run build && npx gh-pages -d dist"

# 4. Разверните
npm run deploy
```

#### Docker развертывание

**Dockerfile:**
```dockerfile
FROM node:18-alpine

WORKDIR /app

# Копируем package files
COPY package*.json ./

# Устанавливаем зависимости
RUN npm ci

# Копируем весь проект
COPY . .

# Строим приложение
RUN npm run build

# Используем lightweight HTTP сервер
FROM node:18-alpine
RUN npm install -g http-server
WORKDIR /app
COPY --from=0 /app/dist .

EXPOSE 3000

CMD ["http-server", "-p", "3000"]
```

**Построение и запуск:**
```bash
# Построить image
docker build -t continuum-careers .

# Запустить контейнер
docker run -p 3000:3000 continuum-careers

# Откройте http://localhost:3000
```

### CI/CD пайплайны

#### GitHub Actions Пример

```yaml
name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run check

      - name: Build
        run: npm run build

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'
```

---

## ✨ Лучшие практики

### Разработка

#### Структура кода

1. **Компонентная организация**
   - Один компонент = один файл
   - Логичные имена (PascalCase для компонентов)
   - Близкие по функциям компоненты в папках

2. **Типизация TypeScript**
   ```astro
   ---
   interface Props {
     title: string;
     count?: number;
     items: Array<{ id: string; label: string }>;
   }

   const { title, count = 0, items } = Astro.props;
   ---
   ```

3. **Разделение concerns**
   - HTML логика в component файле
   - Стили в `<style>` блоке
   - Утилиты в отдельных файлах если нужны

#### Производительность

- ✅ Используйте static generation (SSG)
- ✅ Оптимизируйте изображения (WebP, AVIF)
- ✅ Минимизируйте JavaScript
- ✅ Используйте CSS классы вместо inline стилей
- ❌ Избегайте больших зависимостей
- ❌ Избегайте N+1 запросов

#### Доступность (a11y)

```astro
<!-- ✅ Хорошо -->
<button aria-label="Toggle menu" class="menu-button">
  <span aria-hidden="true">☰</span>
</button>

<!-- ❌ Плохо -->
<div onclick="toggleMenu()" class="fake-button">Menu</div>
```

Рекомендации:
- Используйте семантический HTML (`<button>`, `<nav>`, `<main>`)
- Добавляйте `alt` текст к изображениям
- Убедитесь, что цветовой контраст достаточен
- Тестируйте с screen readers

#### SEO оптимизация

```astro
---
// Используйте правильные meta теги
const title = "Continuum Careers - AI Career Co-pilot";
const description = "Transform your career with AI-powered guidance...";
const ogImage = "https://example.com/og-image.png";
---

<head>
  <meta name="description" content={description} />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:image" content={ogImage} />
  <link rel="canonical" href="https://continuum.careers" />
</head>
```

### Git workflow

#### Branch naming
```
feature/description      - новые функции
fix/description         - исправления багов
docs/description        - документация
refactor/description    - рефакторинг
test/description        - тесты
```

#### Commit messages
```
[type]: Краткое описание

Детальное описание того, почему это изменение нужно было внести.
Может быть несколько строк.

[type]: feat, fix, docs, style, refactor, test, chore
```

#### Pull Request процесс
1. Создайте feature ветку
2. Сделайте изменения
3. Напишите осмысленные commit messages
4. Создайте PR с описанием
5. Запросите review
6. Адресуйте комментарии
7. Мерджьте после approval

---

## 🔧 Устранение неполадок

### Частые проблемы и решения

#### Проблема: `npm install` не работает

```bash
# Решение 1: Очистите npm кэш
npm cache clean --force

# Решение 2: Удалите node_modules и переустановите
rm -rf node_modules package-lock.json
npm install

# Решение 3: Используйте другой npm registry
npm config set registry https://registry.npmjs.org/
```

#### Проблема: Tailwind стили не применяются

```bash
# Проверьте конфигурацию путей в tailwind.config.mjs
# Убедитесь, что указаны правильные пути:
content: [
  './src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'
]

# Очистите Tailwind кэш
rm -rf .next
npm run dev
```

#### Проблема: Dark mode не работает

```javascript
// Убедитесь, что скрипт инициализации выполняется ДО загрузки контента
const html = document.documentElement;
const isDark = localStorage.getItem('theme') === 'dark';
if (isDark) html.classList.add('dark');
```

#### Проблема: Build tidak berhasil dengan ошибкой TypeScript

```bash
# Проверьте типы
npm run check

# Просмотрите детали ошибки
npm run build -- --verbose

# Временно отключите проверку типов (не рекомендуется)
# Отредактируйте astro.config.mjs
```

#### Проблема: Медленное выполнение `npm run dev`

```bash
# 1. Обновите зависимости
npm update

# 2. Очистите кэш
rm -rf .astro
npm run dev

# 3. Проверьте использование диска
du -sh node_modules/

# 4. Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

#### Проблема: Изображения не загружаются в production

```astro
<!-- ❌ Неправильно -->
<img src="../public/image.png" />

<!-- ✅ Правильно -->
<img src="/image.png" />

<!-- ✅ Лучше - используйте Astro Image -->
<Image src={import('../images/image.png')} alt="description" />
```

#### Проблема: Стили конфликтуют между компонентами

```astro
<!-- ✅ Используйте CSS модули или scoped стили -->
<style>
  :global(.dark) .my-component {
    @apply bg-gray-900;
  }

  .component-specific {
    @apply text-red-500;
  }
</style>
```

### Логирование и отладка

#### Логирование на сервере (build time)

```astro
---
console.log('Это выполняется при сборке');
console.log('Переменные окружения:', import.meta.env);

// Условное логирование
if (import.meta.env.DEV) {
  console.log('Режим разработки');
}
---
```

#### Логирование на клиенте (runtime)

```astro
---
// Этот код выполняется в браузере
---

<script>
  console.log('Загружено на клиенте');

  // Отладка Tailwind
  window.tailwindConfig = {
    /* ваша конфигурация */
  };
</script>
```

#### Использование DevTools

```javascript
// Проверьте примененные классы
document.documentElement.classList;

// Проверьте тему
localStorage.getItem('theme');

// Проверьте размер экрана
window.innerWidth;
```

---

## 📚 Дополнительные ресурсы

### Официальная документация

- **Astro Docs:** https://docs.astro.build
- **Tailwind CSS:** https://tailwindcss.com/docs
- **TypeScript:** https://www.typescriptlang.org/docs
- **MDN Web Docs:** https://developer.mozilla.org

### Инструменты и утилиты

- **VS Code Extensions:**
  - Astro (Official)
  - Tailwind CSS IntelliSense
  - TypeScript Vue Plugin (Volar)
  - ESLint
  - Prettier

- **Online Tools:**
  - [Tailwind Play](https://play.tailwindcss.com)
  - [Color Picker](https://colorpicker.com)
  - [Can I Use](https://caniuse.com)

### Сообщество и поддержка

- **GitHub Issues:** https://github.com/withastro/astro/issues
- **Discord Servers:**
  - Astro: https://astro.build/chat
  - Tailwind CSS: https://discord.gg/tailwindcss
- **Stack Overflow:** Тегируйте вопросы как `astro` и `tailwindcss`

---

## 🎓 Запуск в развитии

### День 1: Установка и ознакомление

```bash
# Клонируйте репозиторий (уже сделано)
cd continuum_careers

# Установите зависимости
npm install

# Запустите dev сервер
npm run dev

# Откройте http://localhost:3000 в браузере
# Исследуйте интерфейс, обратите внимание на:
# - Макет и структуру
# - Dark mode переключение
# - Mobile responsiveness
```

### День 2: Внесение изменений

```bash
# Модифицируйте компонент (например, Hero.astro)
# Сохраните файл
# Dev сервер автоматически перезагружает

# Измените цвета в tailwind.config.mjs
# Проверьте результат в браузере

# Добавьте новый компонент
# Экспортируйте его из index.astro
```

### День 3: Production готовность

```bash
# Выполните проверку типов
npm run check

# Создайте production build
npm run build

# Протестируйте локально
npm run preview

# Разверните на платформу (Vercel/Netlify/etc)
```

---

## 📝 Версионирование и обновления

### Семантическое версионирование

```
MAJOR.MINOR.PATCH
 3.4.0

MAJOR: Несовместимые изменения API
MINOR: Новые функции, обратно совместимые
PATCH: Исправления багов
```

### Обновление зависимостей

```bash
# Проверьте текущие версии
npm list

# Проверьте доступные обновления
npm outdated

# Обновите все minor версии
npm update

# Обновите major версию конкретного пакета
npm install astro@latest

# Проверьте совместимость
npm audit

# Зафиксируйте стабильные версии
npm ci
```

---

## ✅ Чеклист для нового разработчика

- [ ] Установлены Node.js 18+ и npm 9+
- [ ] Репозиторий клонирован локально
- [ ] `npm install` успешно завершен
- [ ] `npm run dev` работает, сайт доступен на localhost:3000
- [ ] Понимаю структуру проекта и назначение папок
- [ ] Знаю, как запускать build (`npm run build`)
- [ ] Знаю, как менять стили (Tailwind классы)
- [ ] Знаю, как добавлять новые компоненты
- [ ] Имею VS Code с Astro расширением
- [ ] Прочитал этот документ полностью

---

## 🤝 Вклад в проект

### Как внести вклад

1. **Создайте feature ветку**
   ```bash
   git checkout -b feature/description
   ```

2. **Сделайте изменения**
   ```bash
   # Модифицируйте файлы
   npm run dev  # Тестируйте локально
   ```

3. **Создайте commit**
   ```bash
   git add .
   git commit -m "feat: добавлена новая функция"
   ```

4. **Отправьте в репозиторий**
   ```bash
   git push origin feature/description
   ```

5. **Создайте Pull Request**
   - Описание что было изменено
   - Причина изменения
   - Скриншоты (если UI изменения)

### Код стиль

- Используйте TypeScript для типизации
- Следуйте существующему стилю кода
- Комментируйте сложную логику
- Добавляйте alt текст к изображениям
- Убедитесь, что компонент работает в dark mode

---

## 📄 Лицензия и права

Этот проект является собственностью команды Continuum Careers.

---

## 🙋 FAQ

### Q: Как добавить новую страницу?

A: Создайте новый файл в `src/pages/`. Astro автоматически создаст маршрут:
```astro
// src/pages/about.astro
---
import Layout from '../layouts/Layout.astro';
---

<Layout>
  <h1>About Us</h1>
</Layout>
```

### Q: Как использовать глобальные переменные?

A: Используйте import.meta.env:
```astro
---
const apiUrl = import.meta.env.PUBLIC_API_URL;
const isDev = import.meta.env.DEV;
---
```

### Q: Как подключить API?

A: Используйте fetch в code fence:
```astro
---
const data = await fetch('https://api.example.com/data')
  .then(res => res.json());
---

<div>{data.name}</div>
```

### Q: Как добавить аналитику?

A: Добавьте скрипт в Layout.astro:
```astro
<script src="https://cdn.example.com/analytics.js"></script>
```

### Q: Как работает кэширование?

A: Астро генерирует статический HTML. Для кэширования используйте:
```javascript
<meta http-equiv="Cache-Control" content="max-age=3600">
```

---

## 📞 Контакты и поддержка

- **Email:** support@continuum.careers
- **GitHub Issues:** https://github.com/yourorg/continuum_careers
- **Discord Community:** https://discord.gg/continuum

---

**Последнее обновление:** 12 февраля 2026 г.

**Версия документации:** 1.0.0

---

Made with ❤️ by Continuum Careers Team
